# Aggregate Function (Count, sum, avg, max ,min)
# Using Count 
-- select count(Id) From empname Where age > 20;

# using sum Function
-- Select sum(age) from empname;

# Using Avg Function 
-- Select avg(age) As "Avg Age" from empname;

# Using Min Function
-- select min(age) As "Min Age" from empname;

# Using max Function
select max(age) as "Max Age" from empname